extends ../layouts/default

block content
	.container: .jumbotron
		h1 Welcome
		br
		br
		p This is <a href='http://nrss-staging.zariya.co.za/' target='_blank'>NRSS</a> CMS.
		hr
		if user && user.canAccessKeystone
			p
				a(href='/keystone').btn.btn-lg.btn-primary Open the Admin UI
		else
			p
				a(href='/keystone/signin', style='margin-right: 10px').btn.btn-lg.btn-primary Sign in
				|  to use the Admin UI.
		hr
		br
		br
		p By: T
